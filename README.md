# Forge Arena

A deterministic, build-driven fantasy battle simulator.

## Development

```bash
npm install
npm run dev
```

Quality checks:

```bash
npm test
npm run build
```

The current architecture decisions are recorded in [`docs/system-contract.md`](docs/system-contract.md).
